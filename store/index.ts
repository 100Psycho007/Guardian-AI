// Placeholder for global state management (e.g., Zustand, Jotai, Redux).
// Implement your preferred store here when needed.

export type AuthState = {
  isSignedIn: boolean;
};

export const authState: AuthState = {
  isSignedIn: false,
};
